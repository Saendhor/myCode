#ifndef BUCKETSORT_H
#define BUCKETSORT_H

#include "../../list/node.h"
#include "../../misc/findmin.h"

float bucketsort(float* input) {
    int size = (int) sizeof(input) / sizeof(float);
    node_t* buckets[size];

    return 0;
}
#endif